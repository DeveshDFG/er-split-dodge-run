; separate_roll_and_sprint — ME3 bind folder
; Normal play: separate_roll_and_sprint.dll + this INI + HKS (no libER.dll).

dash_key=LeftShift
enable_menu_patch=0

; Sprint source of truth is dash_key above (GetAsyncKeyState via HKS native).
; Do not assign the same physical key to another action.

; F10 in-game: re-read dash_key
